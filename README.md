# MythCrit

MythCrit — это тёмное веб-приложение VTT (virtual tabletop), созданное на React + Vite + PixiJS.

## 🚀 Запуск проекта

```bash
npm install
npm run dev
```

Приложение будет доступно по адресу [http://localhost:5173](http://localhost:5173).

## 🗺 Добавление своих карт

Положите изображение карты в каталог `public/maps/` и обновите настройки карты через интерфейс. Файл-заглушка по умолчанию находится в `public/maps/default.jpg`.

## ⌨️ Горячие клавиши

- `Space` + ЛКМ или ПКМ — панорамирование карты
- Колесо мыши — зум карты
- `G` — переключение сетки
- `F` — переключение тумана
- `R` — включение линейки
- `Ctrl + 0/1/2` — смена режима вписывания карты (fitMode)
- `Ctrl + S` — экспорт текущей сцены
- `Tab` — показать/спрятать боковую панель
- `Ctrl + Alt + Backspace` — паник-сброс (очистка сохранений и перезапуск)

## 🧭 Управление картой и сеткой

- Вкладка «Карта» позволяет включить трансформационные ручки. Потяните за края/углы, чтобы растянуть или повернуть карту. Удерживайте `Shift`, чтобы масштабировать пропорционально, и `Ctrl`, чтобы переключиться в режим скоса.
- Калибровка сетки: нажмите «Калибровать сетку», кликните две точки на карте и укажите количество клеток между ними — размер, офсеты и поворот сетки пересчитаются автоматически.
- Размер сетки автоматически клампится в диапазон 8–512 px, а прозрачность — от 0 до 1.
- При импорте слишком большой карты изображение автоматически даунскейлится до безопасного размера (не больше `MAX_TEXTURE_SIZE`). В интерфейсе появится уведомление.

## 🎭 Токены

- У каждого токена доступны позиция, масштаб, поворот и флаг блокировки. Заблокированные токены нельзя перемещать и трансформировать.
- Включите «Снэп к сетке» во вкладке «Токены», чтобы привязывать перемещения и гизмо-трансформации к текущей сетке с учётом офсетов и поворота.
- Выбранный токен отображает мини-гизмо (перемещение, масштабирование с `Shift`, поворот с мелким шагом при зажатом `Alt`).

## 📁 Кампании и сохранения

- Вкладка «Сцена» содержит менеджер кампаний: создавайте, дублируйте, удаляйте и экспортируйте кампании (`.json`).
- Сохраняйте текущую сцену в активную кампанию и загружайте любые сохранённые сцены. MythCrit автоматически запоминает последнюю сцену и предлагает «Продолжить» на стартовом экране.
- Экспорт/импорт сцен остаётся доступным отдельно — файлы полностью совместимы с ранними версиями.

## 🎨 Тема и стили

Основные цвета и тема определены в `src/style.css`. Можно изменить фон, текст и акцентные цвета, а также стили компонентов.
